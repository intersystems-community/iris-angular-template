version: '3'
services:
  iris:
    build: 
      context: .
    command: --check-caps false
    ports: 
      - 8080:52773
      - 4200:4200
    volumes:
      - ./:/irisdev/app
      - /irisdev/app/ng/app/node_modules